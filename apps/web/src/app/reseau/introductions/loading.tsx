import { ListSkeleton } from '@/components/network/NetworkSkeletons';

/** ISE-045 (liste) — squelette calque sur la mise en page reelle (D-93). */
export default function IntroductionsLoading() {
  return <ListSkeleton />;
}
