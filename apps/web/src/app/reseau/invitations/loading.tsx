import { ListSkeleton } from '@/components/network/NetworkSkeletons';

/** ISE-041 — squelette calque sur la mise en page reelle (D-93). */
export default function InvitationsLoading() {
  return <ListSkeleton />;
}
